## Summary

<!-- What changed, and why. Link the task or issue if there is one. -->

## Checklist

- [ ] Changes are on a branch named `agent/<task-slug>` (never directly on `main`).
- [ ] `node --test test/` exits 0.
- [ ] `python3 -m http.server 8000` serves `index.html` without errors.
- [ ] No third-party dependencies were added.
- [ ] `AGENTS.md` and `README.md` were updated if layout or behaviour changed.

## Testing

<!-- Commands run and their results, e.g. `node --test test/` -> N passed. -->
